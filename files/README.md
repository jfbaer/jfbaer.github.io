# Placeholder assets

Replace these before publishing:

- `cv.pdf` — your CV. Overwrite this placeholder PDF.
- `header.png` — optional header image referenced by `index.html`. Drop a
  PNG here, or delete the `<figure class="header-well">` block in
  `index.html` if you don't want a header image. (No `header.png` is
  committed yet, so the image will 404 until you add one or remove the block.)
